import React, { Component } from 'react'

export default class PlayerRecap extends Component {
    render() {
        return (
            <div>
                Waiting for next matchup
            </div>
        )
    }
}
